# stop arduino-router before using serialport: sudo systemctl stop arduino-router
# disable arduino-router so it won't run on startup: sudo systemctl disable arduino-router
# enable arduino-router so it runs on startup: sudo systemctl enable arduino-router
# start arduino-router so it runs on startup: sudo systemctl start arduino-router